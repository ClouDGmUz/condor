/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-domain.com'], // Replace with your actual domain if hosting images externally
  },
};

export default nextConfig;
